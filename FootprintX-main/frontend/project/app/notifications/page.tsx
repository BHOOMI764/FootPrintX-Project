export default function NotificationsRemoved() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Notifications — Removed</h1>
        <p className="text-sm text-muted-foreground mt-2">Notifications were removed per project configuration.</p>
      </div>
    </div>
  );
}
